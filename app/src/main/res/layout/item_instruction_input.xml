<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="8dp">

    <EditText
        android:id="@+id/instructionItemField"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="#EEEEEE"
        android:paddingStart="12dp"
        android:paddingEnd="40dp"
        android:hint="@string/addNewInstruction"
        android:imeOptions="actionDone"
        android:autofillHints="Add More"
        android:singleLine="true"
        android:inputType="text"
        android:textColor="@android:color/black"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <ImageView
        android:id="@+id/instruction_clear_3"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginEnd="8dp"
        android:contentDescription="@string/clearInputIcon"
        android:src="@drawable/ic_clear_circle"
        app:layout_constraintEnd_toEndOf="@+id/instructionItemField"
        app:layout_constraintTop_toTopOf="@+id/instructionItemField"
        app:layout_constraintBottom_toBottomOf="@+id/instructionItemField" />
</androidx.constraintlayout.widget.ConstraintLayout>